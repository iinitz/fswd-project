import { React, Fragment } from 'react'
import { useQuery } from '@apollo/client'
import { ORDER_QUERY } from '../../graphql/orderGetbyUser'

const Order = () => {
    return(
     <h1>Order Page</h1>
    )
}

export default Order